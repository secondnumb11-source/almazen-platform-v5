VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-public-key

# Only needed for local scripts/tooling that talk to Supabase directly.
# Edge Function secrets (RESEND_API_KEY, GEMINI_API_KEY, EMAIL_FROM, SUPABASE_SERVICE_ROLE_KEY, ...)
# are configured separately via `supabase secrets set`, NOT via this file.
